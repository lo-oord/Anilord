package org.manga.peak.favourites.domain.model

import org.manga.peak.core.model.MangaSource

data class Cover(
	val url: String?,
	val source: String,
) {
	val mangaSource by lazy { MangaSource(source) }
}
